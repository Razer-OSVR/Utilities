# templates/commit.template
Here you can find a default commit template to apply standard on commit messages.

## How to set it as default template?
Copy the file attached and define it as default template as follow:

```
git config --global commit.template <path_to_file>
```
